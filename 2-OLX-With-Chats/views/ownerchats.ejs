<% for(var i=0; i < messages.length; i++){ %>

<% for(var j=0; j < messages[i].length; j++){ %>
<h4>
    <% if(messages[i][j].from == 'buyer') { %>
    User:
    <% }else{ %>
    You:
    <% } %>
    <%=messages[i][j].message %>
</h4>
<% } %>
<form method="post">
    <input type="hidden" name="buyerid" value="<%=messages[i][0].buyerId %>" />
    <input name="msg" />
    <button type="submit">send</button>
</form>
<% } %>